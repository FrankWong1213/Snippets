location ~ .*.(gif|jpg|jpeg|png|bmp|swf)${
       expires      30d;
       valid_referers none blocked domain.com*;
       if ($invalid_referer) {
       rewrite ^/ https://domain.com/404.html;#return 404;
       }
}